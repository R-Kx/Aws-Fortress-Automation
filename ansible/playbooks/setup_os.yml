
- name: Prepare Servers for Docker(Docker-Compose) & ECR
  hosts: all
  become: true
  vars_files:
    - "../group_vars/all/vars.yml"
    - "../group_vars/all/vault.yml"
  
  tasks:
    - name: Check if vars are loaded
      debug:
        var: hostvars[inventory_hostname]

    - name: Update apt and install relevant libraries
      apt:
        name:
          - apt-transport-https
          - ca-certificates
          - curl
          - software-properties-common
          - python3-pip
          - unzip
          - python3-docker
        update_cache: yes

    - name: Add Docker GPG Key
      apt_key:
        url: https://download.docker.com/linux/ubuntu/gpg
        state: present
    
    - name: Add Docker Repository
      apt_repository:
        repo: "deb [arch=amd64] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
        state: present

    - name: Install Docker Engine
      apt:
        name: 
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-compose-plugin
        state: present
        update_cache: yes

    - name: Ensure Docker Is Running And Enabled
      service:
        name: docker
        state: started
        enabled: yes

    - name: Add ssm-user to docker group
      user:
        name: ssm-user
        groups: docker
        append: yes

    - name: Install AWS CLI
      shell: |
        curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
        unzip -o awscliv2.zip
        ./aws/install --update
      args:
        creates: /usr/local/bin/aws

    - name: Create Directory For App
      file:
        path: /opt/flask-app
        state: directory
        owner: ssm-user
        group: docker
        mode: '0755'

    - name: Deploy docker-compose from template
      template:
        src: ../templates/docker-compose.yml.j2
        dest: /opt/flask-app/docker-compose.yml
        owner: ssm-user
        group: docker
        mode: '0644'